import Testing
@testable import ThreeWood

/// Real tests arrive with RankingEngine in milestone M4; this keeps the test
/// target building from day one.
struct RankingEngineTests {
    @Test func placeholder() {
        #expect(Bool(true))
    }
}
