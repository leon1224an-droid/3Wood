# Handoff — 2026-08-05

State of 3Wood at the point of App Store submission prep. Written so someone
picking this up cold (including a future session) knows what is done, what is
blocked on whom, and which traps have already been paid for.

## Where the app is

- **Release candidate: build 1.0 (6)**, uploaded to App Store Connect and
  processing. Builds 3–5 are superseded; distribute 6.
- **Live on TestFlight** with an external "Friends" group.
- `main` is pushed and clean. Full suite green: **17 unit + 10 UI tests**.
- Hosted Supabase (`tivzyorqxauwbnczicst`) is migrated through **00220** and
  `ACTIVE_HEALTHY`. Verified by signing in as the demo account and calling every
  RPC build 6 depends on — all 200, including `course_visits` and
  `my_ranked_courses`' `last_played_on` / `visit_count`.

## Blocked on the user — App Store Connect web forms only

I cannot reach these; they need the account owner.

1. **App Privacy questionnaire** — answer key in `docs/appstore-listing.md`.
   Note Contacts and Location are correctly answered **No**: contact names never
   leave the device, and location is never sent to the backend.
2. **Age rating questionnaire** — answer key in the same file. Expect **12+**;
   Apple raises it for any UGC app regardless of moderation.
3. **Create the App Store version record and attach build 6.** The
   *App Review Information* section (where the demo credentials go) does not
   exist until that version exists — this caused confusion once already, and it
   is NOT under "App Information".
4. **Paste listing copy** from `docs/appstore-listing.md` (rewritten 2026-08-05)
   and upload the six screenshots from `docs/appstore/screenshots/`.

## Decisions still open

| Decision | Why it matters | Recommendation |
|---|---|---|
| Supabase Pro, $25/mo | Free tier has **no backups** and pauses after a week idle. At launch, real users' rounds, photos and comments have no recovery path. | Do it before submitting |
| Rotate the Supabase DB password | Passed through chat twice | Do it |
| Custom domain | Baked into invite links (`Invite.swift`), so changing later needs an app release | Decide now or accept the cost |
| Email provider | Gmail SMTP caps at 30/hour; a launch spike silently drops sign-up and reset mail | Resend free tier covers 3k/mo |
| Slogan | "Golf is meant to be shared." is live everywhere | Bless or change before submission |
| Crash reporting | There is **none**. A real user's crash is currently invisible beyond Xcode Organizer's sparse opt-in data | Worth adding before launch |

## Costs as traction grows

Apple $99/yr, Supabase Pro $25/mo, domain ~$12/yr, Resend $0–20/mo. Everything
is flat **except photos**: the bucket is public, so egress is charged per
thumbnail *view*, not per upload. ~200–400 KB per photo after downscaling; Pro
includes 250 GB egress, then $0.09/GB. A few hundred golfers is ~$25/mo flat.
If photos take off, the lever is thumbnail caching or a smaller stored size.

## Traps already paid for — do not re-learn these

- **Rebuilding a SQL function**: always start from its LAST `create or replace`
  across all migrations, not where it was born. `leaderboard()` silently lost
  its blocked-user filter this way and shipped to production.
- **Adding a defaulted parameter** to an existing function creates an
  *overload*, not a replacement — `drop function` the old signature first, or
  one-arg calls fail with "is not unique".
- **Date columns must be returned as text** (`to_char(..., 'YYYY-MM-DD')`). The
  supabase-swift decoder only parses full ISO-8601 timestamps; a bare
  `"2026-08-05"` throws at runtime. `PlayDate` is the only place that
  reads/writes them.
- **`.listRowBackground` on a `List` does not propagate to its rows** — it must
  go on each `Section`.
- **Multiple `.sheet(isPresented:)` on one view is unreliable.** This is what
  made "Add photos" silently do nothing. Use one `.sheet(item:)` with an enum.
  (But note: two stacked sheets in `ProfileView` *do* work — verified by test.
  Don't rewrite on assumption.)
- **`supabase db reset` destroys anything not in `seed.sql`.** Check what's in
  the local DB first. Fixture accounts and their data now live in `seed.sql`
  precisely so a reset is reproducible.
- **Only ONE simulator may be booted** during a UI-test run. A second booted
  device fails most of the suite with no code cause, which reads exactly like a
  regression.
- **Reactions toggle**, so any UI test touching them must clean up after itself
  or the next run deletes what it then looks for.
- **Production DB writes are gated** — the user must run `supabase db push`
  themselves.
- **A simulator build from Xcode is DEBUG, and DEBUG points at localhost**
  (`Supa.swift:13`). Rebuilding the App Review demo account through the
  simulator therefore creates it on the *local* stack, not on hosted — the app
  signs in happily and the account looks fine, while the backend Apple actually
  reviews still has nothing. Use `scripts/seed_review_account.py`, which always
  talks to hosted. Only a Release build (or TestFlight) reaches hosted from a
  device.

## Local dev

- Needs OrbStack running; `supabase start`, Studio on :54323.
- Fixture accounts (all `@example.com`, password `testpass123`): `birdie_ben`
  (rich data), `mulligan_mike`, `test1`, `fairway_fiona`, `chip_charlie`,
  `putt_pete`.
- ~16.8k courses imported via `scripts/seed_courses.py`.
- App Review demo account: `3woodapp+review@gmail.com` / @demo_golfer. The
  password is deliberately **not** in this repo — it's public. It lives in App
  Store Connect. **Re-verify it signs in before every submission**: reviewers
  test account deletion, which is what killed the previous demo account.

## Feature history

All 12 items from the 2026-08-01 beta feedback round are done — see
`docs/beta-feedback-2026-08-01.md` for the per-item reasoning. Two later rounds
of device feedback are also complete: Slack-style reactions, tagging and
@mentions, photo paging, a pannable course map, round dates with check-ins, and
a companions step in the log flow.
